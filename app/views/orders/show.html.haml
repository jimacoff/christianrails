- if @order

  %h2 Receipt
  %h4 ChristianDeWolf.com eBook Marketplace

  .dotbox

    %p.embolden= @order.created_at.strftime("%B %e, %Y  %I:%M %p")
    %p= @order.payment_id

    %table.product-table.small-width
      %tbody
        - @order.purchases.each do |purchase|
          %tr
            %td= purchase.product.title
            %td= number_to_currency( purchase.price, precision: 2 )

      - if @order.discount > 0
        %tr
          %th= "Discount:"
          %th.greener= number_to_currency( @order.discount, precision: 2 )

      %tr
        %th= "Tax:"
        %th= number_to_currency( @order.tax, precision: 2 )

      %tr
        %th= "Total:"
        %th= number_to_currency( @order.total, precision: 2)
    %p.diminutive All prices expressed in CAD.

    %br
    %p Thank you for your purchase!
    %h4 Wolf Butler Art & Software


  = link_to "Back to receipts", receipts_orders_path

- else
  %h3 Invalid receipt ID.  Ensure you are logged in and try again.

  %br
  = link_to "Back to store", root_path
