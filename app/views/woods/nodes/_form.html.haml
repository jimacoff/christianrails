= form_for @node do |f|
  - if @node.errors.any?
    #error_explanation
      %h2= "#{pluralize(@node.errors.count, "error")} prohibited this woods_node from being saved:"
      %ul
        - @node.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
