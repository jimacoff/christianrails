%h1 Listing All Players

%table
  %thead
    %tr
      %th Username
      %th Stories
      %th
      %th
      %th

  %tbody
    - @players.each do |woods_player|
      %tr
        %td= woods_player.username
        %td= woods_player.stories.collect(&:name)
        %td= link_to 'Show', woods_player
        %td= link_to 'Edit', edit_woods_player_path(woods_player)
        %td= link_to 'Destroy', woods_player, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= render "admin/admin_link"
