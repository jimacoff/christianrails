- if @cart
  :javascript
    var keys = #{@cart.keys};
    for(var i in keys) {
      $('.cart-dot').show();
      addToCart(keys[i]);
    }

:javascript
  function goToProductPage( slug ) {
    window.location = "/" + slug;
  }
