%p.embolden.right= "#{date_of( @post[:title] )}"

= render "blog/posts/blog_#{ @post[:title] }"
= render "blog/post_footer", locals: { post: @post }

%br
%br

= link_to "Back to Archives", archives_blog_index_path, class: "embolden"
