!!! 5
%html
  %head
    %title ghostCRM
    = stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true
    = stylesheet_link_tag "crm/crm", media: "all", "data-turbolinks-track" => true

    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags

    %meta{ name: "viewport", content: "width=device-width, initial-scale=1.0"}

    %meta{ property: "og:image", content: "http://www.christiandewolf.com/crm-title.png" }
    %meta{ property: "og:image:width", content: "526" }
    %meta{ property: "og:image:height", content: "275" }
    %meta{ property: "og:title", content: "ghostCRM -- the CRM tie-in to GHOSTCRIME" }
    %meta{ property: "og:url",   content: "https://www.christiandewolf.com/crm" }
    %meta{ property: "og:description", content: "Sort your goddamn life out." }

    %meta{ name: "twitter:card", content: "summary_large_image" }
    %meta{ name: "twitter:site", content: "@dewolfchristian" }
    %meta{ name: "twitter:title", content: "ghostCRM" }
    %meta{ name: "twitter:description", content: "ghostCRM -- the CRM tie-in to GHOSTCRIME" }
    %meta{ name: "twitter:image", content: "http://www.christiandewolf.com/crm-title.png" }

    %link{ href: 'https://fonts.googleapis.com/css?family=Artifika', rel: 'stylesheet', type: 'text/css' }

    = favicon_link_tag "crm.ico"

  %body.background

    = render "crm/header"

    - if notice
      %p.alert.alert-notice
        = notice
    - if alert
      %p.alert.alert-danger
        = alert

    .crm-panel
      = yield

    = render "crm/footer"

    %h5.right= "v #{app_version}"
