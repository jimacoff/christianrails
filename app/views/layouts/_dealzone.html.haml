- unless !@cart || !@all_products 
  - klass = @cart.size == 0 ? "hidden" : ""

  #dealzone{class: "#{klass}"}
    %p.embolden Your basket:
    - if @all_products.size > 0
      - @all_products.each do |prod|
        = render 'store/dealzone_product', {prod: prod}

    #check_out
      #discount_price.discount
      #total_price.embolden Total: $0.00
      %button{id: "check_out_button"} Check out now
