%h2.center Forgot your password?
%h5.center I don't judge.
= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
  = devise_error_messages!
  .field
    = f.label "Please enter your email:"
    %br
    = f.email_field :email, autofocus: true
  .actions.center
    = f.submit "Send me reset password instructions", class: "store-button bigger"
    %br
    %br
    %span
      #{ link_to "Sign in", new_session_path(resource_name) } | #{ link_to "Sign Up", new_registration_path(resource_name) }
