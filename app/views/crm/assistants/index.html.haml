- if @assistant
  %h4= "I am your Assistant, #{ @assistant.name }. Please allow me to help you."

  %p
    %span Ideas
    |
    %span Reminders
    |
    %span= link_to "Contacts", crm_contacts_path
    |
    %span Obligations
    |
    %span Meetings

  %table.crm-table
    %thead
      %tr
        %th Obligation
        %th For
        %th Due by

    %tbody
      - @obligations.each do |ob|
        %tr
          %td= ob.name
          %td= ob.contact.name
          %td= ob.due_at

- elsif !current_user
  %h1 You aren't signed in!
  %br
    %p
      = link_to 'Sign in', main_app.user_session_path
      or
      = link_to 'Sign up',  main_app.new_user_registration_path
      to access this area.

- else
  %h1 You don't have an Assistant yet!

  // add a form
  %p.center
    = link_to "Create one", crm_assistants_path, method: "post"
