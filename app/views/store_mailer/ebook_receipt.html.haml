%h2 Your receipt from the ChristianDeWolf.com eBook Marketplace:

%p= @order.created_at.strftime("%B %e, %Y  %I:%M %p")
%p= @order.payment_id

%ul
  - @order.digital_purchases.each do |digital_purchase|
    %li
      %p
        - type = digital_purchase.single? ? "ebook" : "5-pack"
        %span= "#{digital_purchase.product.title} #{type}: "
        %span= number_to_currency( digital_purchase.price, precision: 2 )

      - if @order.discount > 0
        %p
          %span= "Discount: "
          %span= number_to_currency( @order.discount, precision: 2 )

      %p
        %span= "Tax: "
        %span= number_to_currency( @order.tax, precision: 2 )

      %p
        %span= "Total: "
        %span= number_to_currency( @order.total, precision: 2)

%p All prices expressed in CAD.

%br
%p Thank you for your purchase!
%p Christian DeWolf, Wolf Butler Art & Software
